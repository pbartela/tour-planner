---
import Layout from "@/layouts/Layout.astro";
import TourDashboard from "@/components/TourDashboard";
import i18next from "i18next";

// TODO: Uncomment this once the authentication flow is implemented
// if (!Astro.locals.session) {
// 	return Astro.redirect("/auth/signin");
// }
---

<Layout title={i18next.t("dashboard.title", { ns: "tours" })}>
  <main class="container mx-auto py-8">
    <h1 class="mb-6 text-3xl font-bold">{i18next.t("dashboard.heading", { ns: "tours" })}</h1>
    <TourDashboard client:load />
  </main>
</Layout>
