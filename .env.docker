# Docker-specific environment variables for E2E tests
# Copy this to .env and update values for your local Supabase instance

# Public environment variables (accessible in client-side code)
PUBLIC_SUPABASE_URL=http://localhost:54321
PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
PUBLIC_DEFAULT_LOCALE=en-US

# Server-only environment variables (never exposed to client)
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional environment variables
OPENROUTER_API_KEY=
SUPABASE_AUTH_URL=http://supabase-auth:9999

# NOTE: When running tests in Docker, the BASE_URL is set to http://app:3000 in docker-compose.yml
# This allows the Playwright container to communicate with the app container via Docker's internal network
