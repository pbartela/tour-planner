# Public environment variables (accessible on client-side)
PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
PUBLIC_DEFAULT_LOCALE=en-US

# Server-only environment variables (never exposed to client)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Optional API keys
OPENROUTER_API_KEY=your-openrouter-api-key-here
GOTRUE_MAILER_TEMPLATES_MAGIC_LINK='<html><body><h2>Magic Link</h2><p>Click the link below to sign in to your account:</p><p><a href="{{ .SiteURL }}/auth/confirm?access_token={{ .TokenHash }}&type=magiclink">Sign In</a></p></body></html>'

# Testing Configuration
# TEST_MODE: Set to "true" to enable production-like rate limits during testing
# This is useful for:
#   - Running integration tests with realistic rate limiting behavior
#   - Testing rate limit edge cases in development
#   - CI/CD pipelines to catch rate limit issues before deployment
# Leave commented out for normal development (uses relaxed limits)
# TEST_MODE=true
