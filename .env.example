# Public environment variables (accessible in client-side code)
PUBLIC_SUPABASE_URL=http://localhost:54321
# For local development, use the JWT-based anon key (starts with eyJ...)
# For hosted Supabase, you can use either:
#   - New publishable key (sb_publishable_...) - recommended
#   - Legacy JWT anon key (eyJ...)
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0
PUBLIC_DEFAULT_LOCALE=en-US

# Server-only environment variables (never exposed to client)
SUPABASE_URL=http://localhost:54321
# For local Supabase development, use the standard JWT service_role key
# For hosted Supabase, you can use either:
#   - New secret key (sb_secret_...) - recommended, easier rotation, better security
#   - Legacy JWT service_role key (eyJ...)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImV4cCI6MTk4MzgxMjk5Nn0.EGIM96RAZx35lJzdJsyH-qQwv8Hdp7fsn3W0YpN81IU

# Optional: Override Supabase Auth URL (defaults to SUPABASE_URL/auth/v1)
# SUPABASE_AUTH_URL=http://localhost:54321/auth/v1

# Email Configuration
# In development mode, emails automatically route to local Mailpit/Inbucket (http://localhost:54324)
# Resend credentials are OPTIONAL in development - only needed for production email testing

# Resend Email Service (for production email templates)
# Get your API key from https://resend.com/api-keys
# OPTIONAL in development/test mode - leave blank to use Mailpit/Inbucket
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx
RESEND_FROM_EMAIL=Tour Planner <noreply@example.com>

# Test Mode (for CI/E2E testing)
# Set to "true" to force email routing to local SMTP server even in production mode
# Used in GitHub Actions to route emails to Mailpit service container
# TEST_MODE=true

# SMTP Server Configuration (for dev/test email routing)
# Only needed if using custom SMTP server or running tests in CI
# Defaults: localhost:54325 (local Supabase Inbucket)
# SMTP_HOST=localhost
# SMTP_PORT=54325

# Optional: OpenRouter AI API (for future AI features)
# OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# E2E Testing (Playwright)
# These tokens are used for authenticated test scenarios
# Generate by running: npx supabase start, then authenticate via UI and extract tokens from browser storage
# TEST_ACCESS_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# TEST_REFRESH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Database URL for E2E tests (defaults to local Supabase)
# TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:54322/postgres
